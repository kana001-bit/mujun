# AGENTS.md — mujun

設計書（Markdown + Mermaid）の中の矛盾を見つける Linter。eslint のように、どのプロジェクトからも `npx mujun` で使えることを目指す。

ここに書くのは、**コードを読んでも分からない判断と約束**だけにする。関数の説明は書かない（コードを読めば分かることを書くと、書いたそばから古くなる）。

## 方針

- **汎用ルールは、どのプロジェクトでも誤検知しにくいものだけにする。** プロジェクト固有の検査は、そのプロジェクトの側で `defineRule` を使って書いてもらう
- **Linter で一番まずいのは、落ちないことではなく誤検知。** 誤検知が続くと、人は警告を全部読まなくなる。拾うか迷うなら、拾わない・`warn` にする・recommended から外す、のどれかにする
- **ルールには必ず壊れた例（`invalid`）を付ける。** 検査が通っても、ルールが効いている証拠にはならない。`pnpm selftest` で、全ルールが自分の壊れた例で落ちることを確かめる
- **検査の前提（FK の別名など）は、設定ファイルではなく設計書の表に書いてもらう。** 設計書を直すときに前提も一緒に目に入り、前提だけが古くなるのを防げる
- **読めない入力を黙って飛ばさない。** 読めない行は `er.errors` のように持っておき、ルールで報告する
- 文章の書き方（表記ゆれ・文体）は textlint の担当。mujun では扱わない

## 構成

- `src/markdown.ts`：Markdown を Doc に読む（mdast。表のセルは元の文字列のまま持つ）
- `src/er.ts`：erDiagram を自前で解析する（Mermaid の公式パーサーは DOM が要るため使わない）
- `src/engine.ts`：Project を組み立て、ルールを走らせる
- `src/config.ts`：`mujun.config.ts` を探して読む
- `src/selftest.ts`：各ルールの `invalid` / `valid` を走らせる。`mutate` の例は、設定ファイルの `files`（実物の設計書）を書き換えて走らせる
- `src/rules/`：組み込みルール。`index.ts` が一覧、`recommended.ts` が既定のセット

Node は `node_modules` の中の `.ts` を読めない。そのため npm に出すものは `dist/` に出力する。`npm pack` / `npm publish` の前に `prepack` が `pnpm build` を走らせる。開発中は `src/*.ts` をそのまま実行する。

## 実装の約束

- ルールは `Project` を読んで `report` するだけ。ファイルを読まない、`console` に書かない、`process` に触らない。I/O は `cli.ts` と `config.ts` だけ
- `report` には `doc` と `line` を付ける。メッセージには、何と何が食い違っているかを実際の値で書く（例: `番号が 1・2・4（期待 1・2・3）`）
- ルールの ID は `<分類>/<名前>`。一度出した ID と option 名は、利用者の設定ファイルに書かれるので変えない
- ドキュメントとコメントは日本語、識別子は英語。コメントには「なぜ」を書く
- 外部依存は増やさない。増やすなら、自前の実装より壊れにくくなるときだけ
- コミットは求められたときだけ

## テストの約束

- **テストは仕様を守るためにある。** ファイル先頭に `// 守る仕様: 〜` と書く。テスト名は仕様の文そのもの（例: `erDiagram: 多重度で親子を決め、属性の行番号を持つ`）。関数名や内部構造を名前にしない
- ルールの振る舞いは、ルール自身の `invalid` / `valid` に書く。`test/` は解析・エンジン・selftest の土台を守る
- 詳しくは `/test-writing`

## 機械が守っているもの・守っていないもの

`pnpm check`（typecheck → ESLint・Prettier → test → selftest）が落とすものは、レビューで見なくてよい。

| 誰が                                             | 何を                                                                                                                                   |
| ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm typecheck` / ESLint（`strictTypeChecked`） | 型、`any`、await 忘れ、`switch` の網羅、`==`、`console`                                                                                |
| `pnpm selftest`                                  | 全ルールに `invalid` があること、`invalid` で**何かしら**報告すること、`valid` で報告しないこと、`mutate` の書き換えが実物に当たること |
| `test/core.test.ts`                              | 組み込みルールが全部 selftest を通ること                                                                                               |
| `test/conventions.test.ts`                       | テストファイルの先頭行に `// 守る仕様: 〜` があること                                                                                  |

まだ機械が見ていないもの（レビューが見る。`/review-checklist`）:

- `invalid` が**狙った理由で**落ちているか。selftest は報告の件数しか見ない。ER 図の書き間違いで `er.errors` に落ちているだけでも通る
- テストの `// 守る仕様:` の中身が仕様になっているか、テスト名が仕様の文になっているか（あるかどうかは機械が見る）
- README のルール表・option 表と、`builtinRules` / `recommended` / `defaultOptions` が一致しているか
- コメント腐り

新しい約束を足すときは、できれば先に `pnpm check` が落ちる形にしてから書く。できないなら、上の「まだ機械が見ていないもの」に足す。

## スキル

|                     | 使うとき                                                |
| ------------------- | ------------------------------------------------------- |
| `/add-rule`         | 組み込みルールを足す・直すとき                          |
| `/test-writing`     | `test/` やルールの `invalid` / `valid` を足す・直すとき |
| `/review-checklist` | 差分をレビューするとき。完了報告の前にも走らせる        |

## 作業の最後に

`pnpm check` を通す。通せなかったら、何が落ちたかをそのまま報告する。
